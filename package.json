{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "preinstall": "true",
    "build": "pnpm run typecheck && pnpm -r --filter @workspace/api-server --filter @workspace/scholr run build",
    "audit:listings": "node scripts/audit-listings.mjs",
    "seo:check-images": "node scripts/check-og-images.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter @workspace/api-server --filter @workspace/scholr --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "esbuild": "0.25.8",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
